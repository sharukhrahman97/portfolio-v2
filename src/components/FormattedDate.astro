---
interface Props {
  date: Date;
}

const { date } = Astro.props as Props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString("en-US", {
      month: "long",
      day: "2-digit",
      year: "numeric",
    })
  }
</time>
